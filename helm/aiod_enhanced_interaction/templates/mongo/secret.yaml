apiVersion: v1
kind: Secret
metadata:
  name: {{ include "aiod-enhanced-interaction.mongo.fullname" . }}-secret
  labels:
    {{- include "aiod-enhanced-interaction.mongo.labels" . | nindent 4 }}
type: Opaque
stringData:
  username: "root"  # Replace with your MongoDB root username
  password: "changeme"  # Replace with your MongoDB root password
