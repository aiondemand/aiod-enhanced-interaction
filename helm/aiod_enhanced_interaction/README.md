# AIoD Enhanced Interaction Helm Chart

Copy `values.override.yaml.sample` to `values.override.yaml` and fill in your secrets.
Then install with: `helm install my-release . -f values.override.yaml`

Required: MongoDB credentials, Milvus password, Mistral API key, MinIO credentials.
